import { LoginForm } from "@/features/auth";

export const metadata = {
  title: "PixelMart - Login to Continue",
  description: "Login to your PixelMart account to continue",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
